{
  "name": "server",
  "version": "1.0.0",
  "description": "This hosts all the server side code.  - Server to host the visualization app itself  - Server side code to fetch performance numbers from URLs",
  "main": "index.js",
  "scripts": {
    "precommit": "lint-staged"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "husky": "^1.2.0",
    "lint-staged": "^8.1.0",
    "prettier": "^1.15.3",
    "tslint": "^5.11.0",
    "tslint-config-prettier": "^1.17.0",
    "tslint-consistent-codestyle": "^1.14.1",
    "tslint-eslint-rules": "^5.4.0",
    "tslint-react": "^3.6.0",
    "typescript": "^3.2.1"
  },
  "lint-staged": {
    "*.{ts,tsx}": [
      "prettier --write --config ../../.prettierrc",
      "tslint --fix",
      "git add"
    ]
  }
}
